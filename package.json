{
  "name": "pareto-api",
  "version": "0.7.0",
  "description": "the API interface definitions for the pareto package",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "u2l": "ncu -u --packageFile package.json && npm install",
    "pubMin": "rm -rf ./dist && tsc && npm version minor && npm publish && git push"
  },
  "keywords": [
    "80-20",
    "functional",
    "system development"
  ],
  "author": "corno",
  "license": "ISC",
  "types": "dist/index.d.ts",
  "prepublish": "tsc",
  "repository": {
    "type": "git",
    "url": "https://github.com/corno/pareto-api.git"
  },
  "files": [
    "dist/*"
  ]
}
